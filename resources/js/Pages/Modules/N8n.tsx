import { Head, Link } from '@inertiajs/react';

type Execution = { id: number; execution_id: string; workflow_name: string | null; status: string | null; error: string | null; started_at: string | null };

export default function N8n({ executions = [] }: { executions?: Execution[] }) {
	return <><Head title="n8n" /><main className="min-h-screen bg-[#10161d] p-8 text-white"><Link href="/" className="text-sm text-cyan-300">&lt;- Overview</Link><h1 className="mt-10 text-4xl font-semibold">n8n</h1><p className="mt-3 text-slate-400">Ejecuciones recientes y workflows que requieren atencion.</p><div className="mt-10 border border-white/10 bg-[#141d26] p-6"><h2 className="font-semibold">Ejecuciones recientes</h2>{executions.length ? <div className="mt-4 divide-y divide-white/10">{executions.map((execution) => <div key={execution.execution_id} className="flex items-center justify-between gap-4 py-4"><div><p className="text-slate-200">{execution.workflow_name ?? `Ejecucion #${execution.execution_id}`}</p><p className="text-xs text-slate-500">{execution.started_at ? new Date(execution.started_at).toLocaleString('es-ES') : 'Fecha no disponible'}</p></div><span className={execution.status === 'success' ? 'text-emerald-300' : 'text-rose-300'}>{execution.status ?? 'Sin estado'}</span></div>)}</div> : <p className="mt-4 text-sm text-slate-500">A la espera del primer snapshot.</p>}</div></main></>;
}
