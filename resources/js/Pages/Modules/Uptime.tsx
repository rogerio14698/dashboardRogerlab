import { Head, Link } from '@inertiajs/react';
type Check = { id: number; subdomain: { name: string } | null; available: boolean; status_code: number | null; response_time_ms: number | null };
export default function Uptime({ checks = [] }: { checks?: Check[] }) { return <><Head title="Uptime" /><main className="min-h-screen bg-[#10161d] p-8 text-white"><Link href="/" className="text-sm text-cyan-300">&lt;- Overview</Link><h1 className="mt-10 text-4xl font-semibold">Uptime</h1><p className="mt-3 text-slate-400">Disponibilidad y latencia de los subdominios configurados.</p><div className="mt-10 border border-white/10 bg-[#141d26] p-6"><h2 className="font-semibold">Chequeos recientes</h2>{checks.length ? <div className="mt-4 divide-y divide-white/10">{checks.map((check) => <div key={check.id} className="flex items-center justify-between gap-4 py-4"><span>{check.subdomain?.name ?? 'Subdominio'}</span><span className={check.available ? 'text-emerald-300' : 'text-rose-300'}>{check.available ? `${check.status_code} · ${check.response_time_ms ?? '-'} ms` : 'No disponible'}</span></div>)}</div> : <p className="mt-4 text-sm text-slate-500">A la espera del primer snapshot. Ejecuta <code>php8.3 artisan monitor:uptime</code>.</p>}</div></main></>;
}
